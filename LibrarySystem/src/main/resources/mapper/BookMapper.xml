<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.library.lms.mapper.BookInfoMapper">
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="getBookId" resultType="java.lang.String" parameterType="int">
        <!-- 具体的sql -->
        SELECT
        book_name
        FROM
        book_info
        WHERE
        book_id = #{bookId}
    </select>

    <select id="getBookInfo" resultType="map" parameterType="int">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        book_info
        WHERE
        book_id = #{bookId}
    </select>


    <select id="selectBook" resultType="com.library.lms.pojo.BookInfo">
        SELECT
        book_id,
        book_name,
        book_sort_id,
        book_author,
        book_price,
        book_type,
        book_publish,
        book_sum,
        book_mark
        FROM
        book_info
        ORDER BY
        LIMIT 10
    </select>


    <insert id="addBook" parameterType="com.library.lms.pojo.BookInfo">
        INSERT INTO
        book_info (
        book_sort_id,
        book_name,
        book_author,
        book_price,
        book_type,
        book_publish,
        book_sum)
        VALUES (
        #{bookSortId},
        #{bookName},
        #{bookAuthor},
        #{bookPrice},
        #{bookType},
        #{bookPublish},
        #{bookSum});
    </insert>

    <update id = "updateBook" parameterType="com.library.lms.pojo.BookInfo" >
        update book_info set book_name = #{book_name} where book_id = #{book_id}
    </update>

    <delete id = "deleteBook" parameterType="int" >
        delete from book_info where book_id = #{book_id}
    </delete>

</mapper>